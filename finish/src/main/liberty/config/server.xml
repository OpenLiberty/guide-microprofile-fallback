<server description="Sample Liberty server">

  <featureManager>
    <feature>jaxrs-2.1</feature>
    <feature>jsonp-1.1</feature>
    <feature>cdi-2.0</feature>
    <feature>mpConfig-1.3</feature>
    <feature>mpRestClient-1.3</feature>
    <!-- tag::mpFaultTolerance[] -->
    <feature>mpFaultTolerance-2.0</feature>
    <!-- end::mpFaultTolerance[] -->
    <!-- tag::mpMetrics[] -->
    <feature>mpMetrics-1.1</feature>
    <!-- end::mpMetrics[] -->
  </featureManager>

  <quickStartSecurity userName="user" userPassword="password"/>
  <keyStore id="defaultKeyStore" location="key.jks" type="jks" password="mpKeystore"/>

  <httpEndpoint host="*" httpPort="${default.http.port}"
    httpsPort="${default.https.port}" id="defaultHttpEndpoint"/>

<!--   <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" /> -->

  <webApplication location="microprofile-fallback.war" contextRoot="/"/>
</server>
